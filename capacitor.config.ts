import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.yarock',
  appName: 'YaRock',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
